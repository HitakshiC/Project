declare module Identifiers {
    const ExpressServer: unique symbol;
    const RouteManager: unique symbol;
    const ConfigParams: unique symbol;
    const DbManager: unique symbol;
    const ParentController: unique symbol;
    const EmailController: unique symbol;
    const IParentService: unique symbol;
    const ParentDbManager: unique symbol;
}
export default Identifiers;
